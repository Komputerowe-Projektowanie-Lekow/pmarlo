### fixed
- Made `KineticImportanceScore.compute` use a π-orthonormal basis of the slow subspace so KIS is invariant to rotations within the slow eigenspace.
- Restricted the spectral computation to states with positive stationary weight and embed those scores back, preventing inactive identity blocks from polluting the slow spectrum.
