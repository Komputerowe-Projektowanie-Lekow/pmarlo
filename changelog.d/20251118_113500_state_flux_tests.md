### fixed
- Updated `_calculate_state_flux` to average the flux over all incident edges, keeping the score symmetric between edge endpoints and eliminating artificial penalties for directionality.

### added
- Expanded the flux metric test suite with cases covering shared edge means, isolated states, hub-vs-bottleneck behavior, transpose invariance, and linear scaling guarantees.
