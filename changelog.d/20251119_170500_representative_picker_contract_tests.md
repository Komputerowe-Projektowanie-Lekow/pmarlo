### added
- Contract-level tests for `RepresentativePicker` that check index consistency, state filtering, per-state limits, and validation errors for features, weights, and method arguments.

### fixed
- Restored the legacy `pmarlo.representative_picker` import path by re-exporting the picker types from the conformations module.
