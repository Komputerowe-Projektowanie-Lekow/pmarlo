### added
- Added contract tests for ``KineticImportanceScore._recluster`` so discrete trajectory splitting, single-cluster labeling, and trajectory-length validation stay consistent across future refactors.
