### added
- Added regression tests that pin down `_compute_eigenvectors` to return left eigenpairs sorted by magnitude, enforce stationary-mode dominance, validate `n_vecs` handling, and preserve complex modes for cyclic systems.
