### changed
- Wrapped diagnostics payloads in Pydantic models (DiagnosticsResult and AutocorrelationDiagnostics) so downstream consumers receive validated schemas while retaining mapping-style access.
