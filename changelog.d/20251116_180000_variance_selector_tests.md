added:
- Behavioral tests covering the variance-explained selector across real, complex, and negative spectra.
fixed:
- Variance-threshold selection now treats equality as requiring the next slow mode and can include all slow modes when necessary.
