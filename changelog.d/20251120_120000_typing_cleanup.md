### changed
- Tightened the MSM API flow by casting to the EnhancedMSM protocol and handling ITS data explicitly, removing the previous attribute-based type ignores.
- Added protocol typing for MDAnalysis writers and stricter FES whitening validation so trajectory output and dataset preparation no longer rely on `type: ignore` suppressions.
