### added
- Added high-level `KineticImportanceScore.compute` contract tests that cover analytic correctness, monotonicity in `k_slow`, ranking behavior, `"auto"` selection delegation, and argument validation.
